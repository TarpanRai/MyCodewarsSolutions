# Check if the ending first arguments strings, ends with
# the second argument string

# Python has a useful function called x.endswith()
def solution(text, ending):
    if text.endswith(ending):
        return True
    else:
        return False

